/**
 * Releases state and memory associated to a DOM Node.
 *
 * @param {Mount} domNode - Valid input node
 */
export default function release(domNode: Mount): void;
import { Mount } from "./util/types";
