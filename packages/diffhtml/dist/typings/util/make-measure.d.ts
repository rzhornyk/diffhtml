/**
 *
 * @param {Mount} domNode
 * @param {ValidInput=} input
 * @return {(name: string) => void}
 */
export default function makeMeasure(domNode: Mount, input?: ValidInput | undefined): (name: string) => void;
export const marks: Map<any, any>;
export const prefix: "diffHTML";
import { Mount } from "./types";
import { ValidInput } from "./types";
